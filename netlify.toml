[build]
  # Use Python 3.11 so Netlify's build image installs the correct version
  environment = { PYTHON_VERSION = "3.11" }
  # No heavy build step; keeps Netlify's build happy
  command = "echo 'No build step required for Python/Flask app'"
  publish = "."
  functions = "netlify/functions"

# Route root requests to the HTML template
[[redirects]]
  from = "/"
  to = "/templates/index.html"
  status = 200

# Map the REST API path to the Netlify Function in production
[[redirects]]
  from = "/api/weather"
  to = "/.netlify/functions/weather"
  status = 200
  force = true

# SPA-style fallback: only for 404s, show index (optional; remove if you add more pages)
# [[redirects]]
#   from = "/*"
#   to = "/templates/index.html"
#   status = 200
#   conditions = {Role = ["not_found"]}

